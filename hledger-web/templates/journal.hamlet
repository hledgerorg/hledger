<h2>
  #{title'}

$if elem AddPermission perms
  <a #addformlink href="#" role="button"
     data-toggle="modal" data-target="#addmodal" title=_{HMsg "Add a new transaction to the journal"}>
    _{HMsg "Add a transaction"}

<div .table-responsive>
  <table .transactionsreport .table .table-condensed>
    <thead>
      <th .date>_{HMsgc "column heading" "Date"}
      <th .description>_{HMsgc "column heading" "Description"}
      <th .account>_{HMsgc "column heading" "Account"}
      <th .amount>_{HMsgc "column heading" "Amount"}

    $forall torig <- items
      <tr .title ##{transactionFrag torig} title="#{showTransaction torig}">
        <td .date nowrap>
          #{show (tdate torig)}
        <td colspan=2>
          #{textElideRight 60 (tdescription torig)}
        <td .amount>

      $forall Posting { paccount = acc, pamount = amt } <- tpostings torig
        <tr .posting>
          <td>
          <td>
          <td>
            &nbsp;
            <a href="@?{acctlink acc}##{tindex torig}" title="#{acc}">
              #{elideAccountName 40 acc}
          <td .amount>
            ^{mixedAmountAsHtml amt}

$if elem AddPermission perms
  ^{addModal AddR j today}
